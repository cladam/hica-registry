.koka
# Track the .hica directory itself (contains .gitkeep so COPY .hica works in Docker)
# but ignore the package cache and build artifacts inside it.
.hica/cache/
.hica/stdlib/
.hica/*.kk
.hica/*.kki

local

examples/*
!examples/*.hc

src/*
!src/*.hc
!src/sqlite_ffi.kk
!src/sqlite-inline.c

tests/*
!tests/*.hc
!tests/*.sh

registry.*
test_reg.db-shm
test_reg.db-wal

# tbdflow intent log (local-only, never committed)
.tbdflow-intent.json
